[project]
name = "tacsim-automation-demo"
version = "0.1.0"
description = "OpenCV client for the TAC ROV simulator automation API"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "opencv-python>=4.10",
]

[tool.uv]
package = false
